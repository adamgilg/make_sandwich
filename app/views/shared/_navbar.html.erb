<div class="navbar">
  <div class="navbar-inner">
    <%= link_to "make_sandwich", root_path, class: "brand" %>

      <ul class="nav pull-left">
        <li><%= link_to "the quotes", quotes_path %>
        <li class="divider-vertical"></li>
        <li><%= link_to "the sandwiches", sandwiches_path %>
        <li class="divider-vertical"></li>
        
        <% if user_signed_in? %>
          <li class="dropdown">
            <a id="drop1" href="#" role="button" class="dropdown-toggle" data-toggle="dropdown">add a...</a>
            <ul class="dropdown-menu" role="menu" aria-labelledby="drop1">
              <li><a href="#new-sandwich" role="button" data-toggle="modal">sandwich</a></li>
              <li><a href="#new-quote" role="button" data-toggle="modal">quote</a></li>
            </ul>
          </li>
          <li class="divider-vertical"></li>
          <li><%= link_to "order history", user_path(current_user.id) %>
        <% end %>
      </ul>

    <ul class="nav pull-right">
      <% if user_signed_in? %>
        <li><%= link_to "sign out", destroy_user_session_path, method: :delete %></li>
      <% else %>
        <li><%= link_to "sign in", new_user_session_path %></li>
        <li class="divider-vertical"></li>
        <li><%= link_to "sign up", new_user_registration_path %></li>
      <% end %>
    </ul>
  </div>
</div>